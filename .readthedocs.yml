version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.11"
  apt_packages:
    - graphviz

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
